name: Deploy to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 👇 Диагностика содержимого workspace
      - name: Show workspace tree
        run: |
          echo "PWD:"
          pwd
          echo "ROOT:"
          ls -la
          echo "---- backend ----"
          ls -la backend || true
          echo "---- docs ----"
          ls -la docs || true
          echo "---- compose files ----"
          ls -la compose.yml compose.dev.yml || true

      - name: Upload files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT }}
          fingerprint: ${{ secrets.SSH_FINGERPRINT }}
          # 👇 Явные glob'ы, чтобы избежать "tar: empty archive"
          source: |
            backend/**
            docs/**
            compose.yml
            compose.dev.yml
            .gitignore
          target: /opt/kitchen
          overwrite: true
          rm: false
          strip_components: 0
          debug: true

      - name: Build & restart with Docker
        uses: appleboy/ssh-action@v1.0.3
        with:
